{
  "name": "relleno-shifts",
  "version": "1.0.0",
  "description": "Employee scheduling system",
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check script.js && node --check api/auth.js && node --check api/employees.js && node --check api/sales.js && node --check api/_security.js",
    "audit": "npm audit --omit=dev"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "vercel": "^59.1.3"
  }
}
